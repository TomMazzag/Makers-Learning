## Setup

```shell
# Clone the repository to your local machine

# Enter the directory
; cd YOUR_PROJECT_NAME

# Install dependencies and set up the virtual environment
; pipenv install

; playwright install

# Create a test and development database
; createdb music_web_app_html
; createdb music_web_app_html_test

#Database can be seeded with provided sql files
```


